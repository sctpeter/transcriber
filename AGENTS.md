### 开发约定

- 每次完成一个任务，就在docs文件夹里面创建number_function.md，例如0001_某功能.md
- 每次完成一个任务，都需要运行专门的测试代码，放在专门的test文件夹中

### 注意

- 可维护性
- 软件运行失败后，不是说静默回退就好了，更应当让用户知道回退了
- 需要取舍的地方，要与用户交流，而不是自己随意想一个方式决策
- 记得把遇到的配置的坑写到README.md中
- 过于细碎的，不会对整个项目造成明显影响的小改动，可以不在docs中写文档。