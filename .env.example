# 本机开发密钥,不要提交真实值(.env 已在 .gitignore 里)。
# make_signing_cert.sh / build_app.sh 首次运行会自动生成 .env 并写入随机密码,
# 一般不需要你手动填这个文件——这份 .env.example 只是给别人看"这里有什么变量"。

# 保护 transcriber-dev.keychain-db 的密码(只用来存放本机代码签名证书,
# 和运行时的 TLS/远程 ASR 完全无关)。
TRANSCRIBER_DEV_KEYCHAIN_PASS=
